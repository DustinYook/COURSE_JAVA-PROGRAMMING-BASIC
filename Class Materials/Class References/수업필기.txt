// 모든 클래스의 조상이 Object 클래스임 => 자연상속 받는 특성존재
// Thread: 실행되는 실타래

// key를 일정한 규칙에 의해 수치로 바꾼 것을 hashcode라 함
// 같은 것을 또 한 번 검증해주어야 함 => equals()를 두 번 해주어야 함
// hashcode -> equals();

// final이 붙어있는 클래스: 상속을 하지 못함 => 자식이 없는 클래스 (상속금지)
